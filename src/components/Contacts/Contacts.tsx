import React from 'react'
import styles from  './styles.module.css'

function Contacts() {
	return (
		<section className={styles.container} id="contacts">Контакты</section>
	)
}

export default Contacts
